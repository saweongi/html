package ch12_exception;

public class ExMethod {
	
	//날짜 문자열을 입력으로 받아 밀리초로 바꿔주는 메소드
	public static long dateMillsec(String date) {
		SimpleDateFormat sdf = new SimpleDateFromat("yyy.MM.dd");
		Date temp = sdf.parse(Date);
		return temp.getTime();
	}
}
